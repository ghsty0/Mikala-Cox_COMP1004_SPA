//testing implementation

/*chordCell(x,y){
    console.log("chording")
    let correctCats = 0
    for (let dy = -1; dy <= 1; dy++){
        for (let dx = -1; dx <= 1; dx++){
            if (dx === 0 && dy === 0){continue;}
            const cell = this.getCell(x+dx,y+dy)
            if (!cell){continue;}
            if (cell.isCat && cell.isFlagged == true){
                correctCats++;
                console.log(correctCats)
            }
        }
    }
}*/

